<!--pages/tabs/task.wxml-->
<view wx:for-items="{{dataList}}">
  <task-item name="{{item.elderName}}{{item.taskName}}" iconPath="/res/task.png" taskTime="{{item.execTime}}" taskId="{{item.taskId}}" />
</view>